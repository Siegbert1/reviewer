install:
python 3.6
github
atom


in folder:
"python -m venv myvenv"  for virtualenvironment

activate virtualenvironment
"pip install --upgrade pip"
"pip install django~=1.11.0"
"pip install python-decouple" later get the .env so you have the secret_key

use the git GUI to clone from https://github.com/siegbert1/reviewer.git to folder
place .env in streber-folder

everything should work now
